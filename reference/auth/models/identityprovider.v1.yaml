title: Identity Provider
description: An external identity provider linked to the user account.
type: object
properties:
  alias:
    type: string
    description: The unique alias identifying the identity provider configuration.
    example: 'google'
  name:
    type: string
    description: The display name of the identity provider.
    example: 'Google'
required:
  - alias
  - name
