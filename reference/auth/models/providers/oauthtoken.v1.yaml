title: Provider OAuth Token
description: >-
  OAuth token from a provider.
type: object
properties:
  accessToken:
    type: string
    format: bytes
  refreshToken:
    type: string
    format: bytes
  tokenType:
    type: string
  expirationTime:
    $ref: '../../../common/models/datetime.v1.yaml'
required:
  - accessToken
