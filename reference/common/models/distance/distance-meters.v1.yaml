title: Distance (meters)
type: object
properties:
  units:
    type: string
    enum:
      - meters
    example: 'meters'
  value:
    type: number
    format: float
    minimum: 0
    maximum: 160934.4
    example: 42
required:
  - units
  - value
