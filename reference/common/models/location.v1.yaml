title: Location
type: object
description: >-
  Location information associated with the resource.
  One or both of `name` and `gps` must be specified.
properties:
  name:
    type: string
    description: A name for the location.
    minLength: 1
    maxLength: 100
  gps:
    $ref: './gps.v1.yaml'
