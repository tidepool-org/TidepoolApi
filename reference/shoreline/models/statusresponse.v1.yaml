title: Status Response
type: object
properties:
  error:
    type: integer
    minimum: 0
    description: Internal `error` code.
  reason:
    type: string
    description: Human readable `reason` for the resulting `code`
  code:
    type: integer
    enum:
      - 400
      - 401
      - 403
      - 409
      - 500
    description: The HTTP status code
required:
  - code
  - reason
description: The status response for a Shoreline request
x-tags:
  - auth
