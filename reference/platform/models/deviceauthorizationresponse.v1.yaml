title: Device Authorization Response
type: object
properties:
  id:
    $ref: ./deviceauthorizationid.v1.yaml
  status:
    type: string
    enum:
      - pending
      - expired
      - successful
      - failed
  verificationCode:
    $ref: ./verificationcode.v1.yaml
required:
  - id
  - status
