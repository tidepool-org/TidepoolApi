title: Report endpoint dates
description: Start and end dates
type: object
properties:
  startDate:
    $ref: ../../common/models/datetime.v1.yaml
    description: startDate cannot be more than 90 days before the endDate
  endDate:
    $ref: ../../common/models/datetime.v1.yaml
    description: endDate cannot be in the future
