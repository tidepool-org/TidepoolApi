type: object
title: tide.v1
description: 'Report of at-risk patients based on specific grouping criteria'
properties:
  config:
    $ref: './tideConfig.v1.yaml'
  results:
    $ref: './tideResults.v1.yaml'
required:
  - config
  - results
