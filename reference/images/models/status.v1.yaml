title: Image Status
description: >-
  The status of an image.
  The status is `available` if and only if the image has content.
  Otherwise, the status is `created`.
type: string
enum:
  - available
  - created
example: 'created'
