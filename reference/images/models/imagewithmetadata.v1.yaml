title: Image with Metadata
description: >-
  An image with just metadata.
type: object
allOf:
  - $ref: './base.v1.yaml'
  - type: object
    properties:
      metadata:
        $ref: './metadata.v1.yaml'
