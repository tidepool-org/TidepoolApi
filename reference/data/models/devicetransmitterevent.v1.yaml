title: 'DeviceTransmitterEvent [Proposed]'
allOf:
  - $ref: ./deviceevent.v1.yaml
  - type: object
    properties:
      subType:
        type: string
        enum:
          - transmitterEvent
      eventType:
        type: string
        enum:
          - expired
          - start
          - stop
x-tags:
  - data
description: A transmitter event
