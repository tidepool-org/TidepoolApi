title: Basal (Abstract)
x-tags:
  - data
description: 'This is an abstract type used to define BasalAutomated, BasalScheduled, BasalSuspend and BasalTemporary'
allOf:
  - $ref: ./base.v1.yaml
  - type: object
    properties:
      deliveryType:
        type: string
        enum:
          - automated
          - scheduled
          - suspend
          - temp
      type:
        enum:
          - basal
        type: string
    required:
      - deliveryType
      - type
