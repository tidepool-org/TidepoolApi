title: Trend
type: object
properties:
  Rate:
    $ref: ./rate.v1.yaml
  Category:
    type: string
    description: 'Categories are annotated in terms of `slow`, `moderate` and `rapid`, since different manufacturers use different arrow glyphs.'
    enum:
      - constant
      - moderateFall
      - moderateRise
      - rapidFall
      - rapidRise
      - slowFall
      - slowRise
x-tags:
  - data
