title: Device Calibration
allOf:
  - $ref: './event.v1.yaml'
  - type: object
    description: A blood glucose device calibration event
    properties:
      subType:
        type: string
        enum:
          - calibration
    required:
      - subType
  - $ref: '../blood/glucose/glucose.v1.yaml'
x-tags:
  - Data
