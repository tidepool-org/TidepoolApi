title: 'DeviceTransmitterEvent [Proposed]'
allOf:
  - $ref: './event.v1.yaml'
  - type: object
    description: A transmitter event
    properties:
      subType:
        type: string
        enum:
          - transmitterEvent
      eventType:
        type: string
        enum:
          - expired
          - start
          - stop
x-tags:
  - Data
