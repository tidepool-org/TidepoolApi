title: Controller Settings
allOf:
  - $ref: ../base.v1.yaml
  - type: object
    description: Controller settings
    properties:
      type:
        type: string
        enum:
          - controllerSettings
      device:
        $ref: ./device.v1.yaml
      notifications:
        $ref: ./notifications.v1.yaml
x-tags:
  - Data
