title: Device Time Change
oneOf:
  - allOf:
      - $ref: ./deviceevent.v1.yaml
      - type: object
        properties:
          subType:
            type: string
            enum:
              - timeChange
          change:
            properties:
              agent:
                type: string
                enum:
                  - automatic
                  - manual
              from:
                $ref: ../../common/models/datetimenotz.v1.yaml
              to:
                $ref: ../../common/models/datetimenotz.v1.yaml
            required:
              - agent
              - from
              - to
            oneOf:
              - type: string
              - type: object
        required:
          - subType
    description: Legacy `timeChange` event
  - allOf:
      - $ref: ./deviceevent.v1.yaml
      - type: object
        properties:
          subType:
            type: string
            enum:
              - timeChange
          from:
            $ref: ./timechangeinfo.v1.yaml
          method:
            type: string
            enum:
              - automatic
              - manual
          to:
            $ref: ./timechangeinfo.v1.yaml
        required:
          - subType
          - method
          - to
    description: A `timeChange` event
x-tags:
  - data
description: A device time change
