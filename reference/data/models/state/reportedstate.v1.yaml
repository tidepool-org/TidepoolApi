title: Reported State
allOf:
  - $ref: '../base.v1.yaml'
  - type: object
    description: Reported states that can have an impact on blood glucose
    properties:
      type:
        type: string
        enum:
          - reportedState
      states:
        $ref: './states.v1.yaml'
required:
  - states
x-tags:
  - Data
