title: Insulin
description: >-
  An insulin delivery event that is not associated with a basal or bolus, 
  usually logged as a contextual event on a CGM receiver or BG meter.
allOf:
  - $ref: '../base.v1.yaml'
  - type: object
    properties:
      dose:
        $ref: './dose.v1.yaml'
      formulation:
        $ref: './formulation.v1.yaml'
      site:
        type: string
        minLength: 0
        maxLength: 100
      type:
        type: string
        enum:
          - insulin
required:
  - type
x-tags:
  - Data
