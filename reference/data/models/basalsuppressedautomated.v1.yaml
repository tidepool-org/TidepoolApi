title: Basal Suppressed Automated
type: object
properties:
  type:
    type: string
    enum:
      - basal
  scheduleName:
    type: string
    description: The name of the basal schedule.
  rate:
    type: number
    format: double
    minimum: 0
    maximum: 100
    description: 'A floating point number >= 0 representing the amount of insulin delivered in units per hour.'
  insulinFormulation:
    $ref: ./insulinformulation.v1.yaml
  deliveryType:
    type: string
    enum:
      - automated
  annotations:
    $ref: ./metadataarray.v1.yaml
x-tags:
  - data
