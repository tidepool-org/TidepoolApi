title: DeviceEvent (Abstract)
allOf:
  - $ref: ./base.v1.yaml
  - type: object
    properties:
      subType:
        type: string
        enum:
          - alarm
          - calibration
          - prime
          - reservoirChange
          - sensorEvent
          - settingsChange
          - status
          - timeChange
          - transmitterEvent
      type:
        type: string
        enum:
          - deviceEvent
    required:
      - subType
      - type
x-tags:
  - data
