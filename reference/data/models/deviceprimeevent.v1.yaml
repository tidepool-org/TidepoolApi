title: Device Prime Event
oneOf:
  - allOf:
      - $ref: ./deviceevent.v1.yaml
      - type: object
        properties:
          primeTarget:
            type: string
            enum:
              - cannula
          subType:
            type: string
            enum:
              - prime
          volume:
            type: number
            format: float
            minimum: 0
            maximum: 10
        required:
          - primeTarget
          - subType
    description: A cannula prime event
  - allOf:
      - $ref: ./deviceevent.v1.yaml
      - type: object
        description: A tubing prime event
        properties:
          primeTarget:
            type: string
            enum:
              - tubing
          subType:
            type: string
            enum:
              - prime
          volume:
            type: number
            format: float
            minimum: 0
            maximum: 100
        required:
          - primeTarget
          - subType
x-tags:
  - data
description: An insulin pump prime event
